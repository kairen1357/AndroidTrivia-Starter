<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/titleFragmet2">

    <fragment
        android:id="@+id/gameFragment"
        android:name="com.example.android.navigation.GameFragment"
        android:label="GameFragment"
        tools:layout="@layout/fragment_game"/>
    <fragment
        android:id="@+id/titleFragmet2"
        android:name="com.example.android.navigation.TitleFragmet"
        android:label="TitleFragmet" >
        <action
            android:id="@+id/action_titleFragmet2_to_gameFragment"
            app:destination="@id/gameFragment" />
        <action
            android:id="@+id/action_titleFragmet2_to_gameFragment2"
            app:destination="@id/gameFragment" />
    </fragment>
</navigation>